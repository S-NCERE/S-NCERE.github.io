function setup() {
    var cnv = createCanvas(800, 800);
    var x = (windowWidth - width) / 2;
    var y = (windowHeight - height) / 2;
    cnv.position(x, y);
    cnv.parent('meme');
    background(0);
    img = loadImage('images/dripDex.jpg');
    img2 = loadImage('images/dripDex.jpg');
}

function draw(){
    //when ya drip go un noticed
    // imgs to big must size down
    
}